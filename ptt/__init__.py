"""web-ptt vertical stripe.

All PTT-specific code lives here:

- ``ptt.backend``    — standalone FastAPI svc (``python -m ptt.backend``).
- ``ptt/components`` — Svelte source consumed by the worktree's Vite frontend.
- ``ptt/static``     — assets shipped into the built bundle (mic-worklet.js).
"""
