{
  "name": "@awm/primitives",
  "version": "0.1.0",
  "private": true,
  "description": "Atomic UI primitives (Button, Card, Input, Tag, Pill, …) plus a standalone gallery that renders one card per primitive. Registered as a frontend-only stripe at /primitives; sibling stripes import the components via @awm/primitives.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": ["**/*.css", "**/*.svelte"],
  "scripts": {
    "build": "vite build"
  },
  "stripe": {
    "frontend": "dist/",
    "prefix": "/primitives"
  },
  "dependencies": {
    "bits-ui": "^1.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.2.0",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
