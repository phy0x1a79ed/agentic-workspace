{
  "name": "@awm/ptt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Push-to-talk vertical stripe: Python FastAPI backend + SvelteKit UI. Hub auto-supervises the backend (port from AWM_STRIPE_PORT_POOL, /healthz gated) and proxies /ptt/_api/* with peer-bearer injection.",
  "scripts": {
    "build": "cd frontend && npm install --no-audit --no-fund && npm run build"
  },
  "stripe": {
    "frontend": "dist/",
    "prefix": "/ptt",
    "backend": {
      "cmd": ["mamba", "run", "-n", "awm", "--no-capture-output",
              "python", "-m", "backend", "--port", "${AWM_SERVICE_PORT}"],
      "health": "/healthz",
      "cwd": "."
    }
  }
}
