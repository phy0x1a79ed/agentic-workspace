{
  "name": "@awm/hello",
  "version": "0.1.0",
  "private": true,
  "description": "Smoke-test stripe. Frontend hits its own backend through the hub-injected proxy path; lets you verify the full register → spawn → health → proxy cycle in a browser without rebuilding the SPA.",
  "type": "module",
  "stripe": {
    "frontend": "dist/",
    "prefix": "/hello",
    "backend": {
      "cmd": ["node", "server.js"],
      "health": "/healthz",
      "cwd": "."
    }
  }
}
