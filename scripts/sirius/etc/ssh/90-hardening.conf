# Installed by scripts/sirius/provision.sh. sshd_config includes this dir at
# the top, so these values win over the stock file.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
AllowUsers tony
MaxAuthTries 4
LoginGraceTime 30
X11Forwarding no
