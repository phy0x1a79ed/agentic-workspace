[pytest]
# The mira API daemon is host tooling, not an awm namespace dist, so it has its
# own pytest config. Run it standalone (aiohttp + pytest-asyncio required):
#   cd awm/services/social/mira && PYTHONPATH=. python -m pytest
asyncio_mode = auto
testpaths = tests
