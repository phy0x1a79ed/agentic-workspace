"""awm compute — the local-compute watchdog.

Keeps one agent from wedging a shared box, without taking away an agent's
freedom to use it. See ``INSTALL.md`` for the shape and ``hub_adapter`` for the
API surface.
"""
