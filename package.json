{
  "name": "awm-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Packages workspace. packages/components/* are importable libraries; packages/pages/* are static bundles served at /ui/<name>; packages/services/* are RPC-over-WS services served at /svc/<name>. Per-package package.json + vite.config.ts are GENERATED from layout — see awm/services/packages/gen.py.",
  "workspaces": [
    "packages/components/*",
    "packages/pages/*"
  ],
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "bits-ui": "^2.18.1",
    "svelte": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
